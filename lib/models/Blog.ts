import mongoose, { Schema, Document } from 'mongoose';

export interface IBlogContent {
  type: 'paragraph' | 'heading' | 'list' | 'quote';
  content: string | string[];
}

export interface IBlog extends Document {
  slug: string;
  title: {
    en: string;
    tr: string;
  };
  excerpt: {
    en: string;
    tr: string;
  };
  content: IBlogContent[];
  date: string;
  readTime: string;
  category: string;
  author: string;
  tags: string[];
  coverImage?: string;
  isPublished: boolean;
  /**
   * Which language edition this post belongs to. Turkish posts are written in
   * Turkish from the start rather than translated, so they are their own
   * documents with their own slugs and their own topics — a translated archive
   * reads like one and ranks like one.
   */
  lang?: 'en' | 'tr';
  /** True for posts written by the automated Groq pipeline (lib/blog-generator). */
  autoGenerated?: boolean;
  createdAt: Date;
  updatedAt: Date;
}

const BlogContentSchema = new Schema({
  type: {
    type: String,
    enum: ['paragraph', 'heading', 'list', 'quote'],
    required: true
  },
  content: {
    type: Schema.Types.Mixed,
    required: true
  }
}, { _id: false });

const BlogSchema = new Schema<IBlog>({
  slug: {
    type: String,
    required: true,
    unique: true,
    trim: true,
    lowercase: true
  },
  title: {
    en: { type: String, required: true },
    tr: { type: String, required: true }
  },
  excerpt: {
    en: { type: String, required: true },
    tr: { type: String, required: true }
  },
  content: [BlogContentSchema],
  date: {
    type: String,
    required: true
  },
  readTime: {
    type: String,
    required: true
  },
  category: {
    type: String,
    required: true
  },
  author: {
    type: String,
    default: 'WatchPulse Team'
  },
  tags: [{
    type: String
  }],
  coverImage: {
    type: String
  },
  isPublished: {
    type: Boolean,
    default: true
  },
  lang: {
    type: String,
    enum: ['en', 'tr'],
    default: 'en'
  },
  autoGenerated: {
    type: Boolean,
    default: false
  }
}, {
  timestamps: true
});

// Indexes for better performance (slug index already defined via unique: true)
BlogSchema.index({ category: 1 });
BlogSchema.index({ isPublished: 1 });
BlogSchema.index({ createdAt: -1 });
// Backs the cron's "how many did we auto-publish today?" guard.
BlogSchema.index({ autoGenerated: 1, createdAt: -1 });
// Every index and category listing is scoped to one language edition.
BlogSchema.index({ lang: 1, isPublished: 1, createdAt: -1 });
BlogSchema.index({ lang: 1, category: 1, isPublished: 1 });

// Dedicated collection: the backend keeps its own `blogs` collection (backend
// schema); the website reads/writes the mirrored website-schema docs in `webblogs`.
export default mongoose.models.Blog || mongoose.model<IBlog>('Blog', BlogSchema, 'webblogs');
