import SiteNotFound from '@/components/SiteNotFound';

/** Unmatched URLs. The segments that call notFound() carry their own boundary. */
export default function NotFound() {
  return <SiteNotFound />;
}
